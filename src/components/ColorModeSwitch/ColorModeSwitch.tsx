import { VStack, HStack, Text } from "@chakra-ui/react";

const ColorModeSwitch = () => {
  return (
    <VStack>
      <HStack>
        <Text>Dark Mode</Text>
      </HStack>
    </VStack>
  );
};

export default ColorModeSwitch;
